'use strict';

var chai = require('chai'),
    expect = chai.expect;

let di = require('./core/DesignInformation');
let awsc = require('./core/AwsConfiguration');
let awsRegion = require('./enum/awsRegion').awsRegion;

chai.should();

describe("Very AWS requestes are handled properly", function() { 
    it("should give me my file", function() { 
        
    });
});